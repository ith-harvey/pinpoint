<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    {{> logout}}
    <a href="/users/{{userId}}/customize">Add Preference</a>
    <h1>{{userName}}</h1>
    <h3>Preferences:</h3>
    {{#each userTags}}
       <p>{{this.name}}</p>
    {{/each}}
  </body>
</html>
